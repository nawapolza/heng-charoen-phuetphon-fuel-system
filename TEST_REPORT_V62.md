# Test Report V62 — Multi-Branch Professional Edition

วันที่ตรวจ: **2026-08-03**  
เวอร์ชัน: **62.0.0**  
Build: `heng-charoen-v62-multi-branch`

## ผลตรวจที่ผ่าน

| รายการตรวจ | ผล |
|---|---|
| Backend JavaScript syntax (`node --check`) | ผ่าน |
| JavaScript/JSX syntax ทุกไฟล์ด้วย TypeScript parser | ผ่าน |
| Relative imports ของ Frontend | ผ่าน |
| JSON ของ package manifests | ผ่าน |
| `package.json` ตรงกับ root metadata ใน `package-lock.json` | ผ่าน |
| Merge conflict markers | ไม่พบ |
| V62 structural verification | ผ่าน |
| Route CRUD สาขา | พบครบ GET/POST/PUT/DELETE |
| Header `X-Branch-Id` จาก Frontend | พบและใช้งาน |
| Compound stock key `branch_id + item_type` | พบ |
| Scope รายการ/สต๊อก/รายงาน/รถ/ผู้ใช้/แจ้งเตือน | พบครบ |
| ย้ายพนักงานข้ามสาขาและรักษาประวัติเดิม | พบครบ |
| เปิดใช้งานสาขา/บัญชีที่เคยปิด | พบครบ |
| Soft delete และเปิดใช้สาขาอีกครั้ง | พบครบ |
| รถใช้ทั่วไปสำหรับพนักงานในสาขา | พบครบ |
| ป้องกันทะเบียนรถซ้ำในสาขาเดียวกัน | พบครบ |
| CSS braces | สมดุล |

## Matrix การเชื่อมโยงหน้า

| หน้า | ขอบเขตสาขา | การทำงานหลัก |
|---|---|---|
| Dashboard | สาขาที่เลือก | สรุปรายการ ค่าใช้จ่าย สต๊อก และแจ้งเตือน |
| จัดการสาขา | ทุกสาขาสำหรับ Owner | เพิ่ม แก้ไข เลือก ปิด และเปิดใช้งานอีกครั้ง |
| คำนวณน้ำมัน | สาขาที่เลือก | ดึงรถและสต๊อกของสาขา |
| บันทึกเติมน้ำมัน | สาขาที่เลือก/สาขาบัญชี | บันทึกรายการและร่างฟอร์มแยกสาขา |
| รายการย้อนหลัง | สาขาที่เลือก | ค้นหา แก้ไข ลบเฉพาะสาขา |
| สต๊อกน้ำมัน | สาขาที่เลือก | เติม ตรวจนับ ตั้งค่า ปรับยอด และประวัติ |
| รายงานบัญชี | สาขาที่เลือก | รายเดือน พิมพ์/PDF และ CSV ระบุสาขา |
| รถและคนขับ | สาขาที่เลือก | CRUD และรถใช้ทั่วไป |
| พนักงาน | สาขาที่เลือก | CRUD บัญชีแยกสาขา |
| แจ้งเตือน | สาขาที่เลือก | อ่านและติดตามแจ้งเตือนเฉพาะสาขา |

## ข้อจำกัดของสภาพแวดล้อมตรวจ

มีการสั่งติดตั้งเพื่อทำ Production Build แล้ว แต่ Registry ของสภาพแวดล้อมนี้เคยตอบกลับ 404 และการติดตั้งรอบสุดท้ายค้างจนหมดเวลารอ จึงไม่สามารถรัน Vite production build จนจบภายในสภาพแวดล้อมตรวจนี้ได้ ไฟล์ `package.json` และ `package-lock.json` ถูกปรับให้ตรงกันแล้ว และ `render.yaml` ใช้ `npm ci` ตามมาตรฐานสำหรับ Build ในสภาพแวดล้อมที่เข้าถึง npm registry ได้

คำสั่งตรวจหลังดาวน์โหลด:

```bash
npm run verify:v62
cd frontend
npm ci --include=dev --no-audit --no-fund
npm run build
```
