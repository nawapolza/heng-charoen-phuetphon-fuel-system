# Release Notes V58 — One Vehicle, Multiple Jobs

## เป้าหมาย
รองรับกรณีรถคันเดียวขึ้นงานหลายงานภายในรายการน้ำมันเดียว โดยแยกระยะทาง เส้นทาง น้ำหนัก รายได้ และสถานะการจ่ายของแต่ละงานได้ครบถ้วน

## สิ่งที่เพิ่ม
- เพิ่มปุ่ม **เพิ่มงาน / คัดลอกงาน / ลบงาน** ในฟอร์มบันทึกน้ำมัน
- แต่ละงานบันทึกชื่องาน จุดขึ้นงาน จุดลงงาน วันที่ขึ้น/ลง ระยะทาง น้ำหนัก รายได้ ผู้จ่าย และสถานะจ่ายได้แยกกัน
- รวมระยะทางทุกงานอัตโนมัติ แล้วใช้คำนวณ `ระยะทางรวม ÷ อัตราประจำรถ = จำนวนลิตรตามเรท`
- รวมค่าเที่ยว เบี้ยเลี้ยง รายได้อื่น น้ำหนักหิน และน้ำหนักไม้สับจากทุกงาน
- ใบสรุปบนเว็บ Popup และไฟล์ PNG แสดงงานหลายงานพร้อมยอดรวม
- Dashboard นับจำนวนงานย่อยจริง แม้รถหนึ่งคันมีหลายงานในรายการเดียว
- สรุปปลายทางกระจายปริมาณน้ำมันตามสัดส่วนระยะทางของแต่ละงาน จึงไม่บวกยอดน้ำมันซ้ำ
- ช่องค้นหาค้นข้อมูลภายในงานย่อยได้ เช่น ต้นทาง ปลายทาง ชื่องาน ผู้จ่าย และหมายเหตุ

## ความเข้ากันได้
- ข้อมูลเก่าที่มีเพียงงานเดียวจะถูกอ่านเป็นงานที่ 1 อัตโนมัติ
- ไม่ต้องย้ายฐานข้อมูล MongoDB และไม่ลบข้อมูลเดิม
- API ยังคงฟิลด์เดิมระดับบนไว้เพื่อรองรับหน้าหรือข้อมูลเวอร์ชันเก่า

## การตรวจสอบ
- Backend ผ่าน `node --check backend/server.js`
- Frontend ทั้ง 21 ไฟล์ผ่าน TypeScript JSX parser โดยไม่พบ syntax error
- ไม่สามารถทำ Vite production build ภายในสภาพแวดล้อมนี้ได้ เพราะ registry ภายในไม่มีแพ็กเกจ dependency บางตัว แต่ source code และ lockfile เดิมถูกเก็บครบสำหรับ build บนเครื่องหรือ Render

## V58.0.1 — Render Merge Conflict Fix

- ลบ Merge Conflict ออกจาก package.json ของ Backend และ Frontend
- ซิงก์เวอร์ชัน package-lock.json เป็น 58.0.1
- เพิ่มตัวตรวจ JSON และ Merge Conflict ก่อน Render build
