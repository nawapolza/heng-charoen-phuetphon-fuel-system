# Git Ready — UX/UI V46

เปิด Terminal ที่โฟลเดอร์โปรเจกต์ แล้วรัน:

```bash
git add .
git commit -m "Redesign delivery form UX UI v46"
git push origin main
```

สำหรับ Render ให้ใช้คำสั่ง Build เดิมของโปรเจกต์:

```bash
npm install; npm run build
```

หรือถ้า Root Directory ตั้งเป็น `frontend`:

```bash
npm install
npm run build
```
