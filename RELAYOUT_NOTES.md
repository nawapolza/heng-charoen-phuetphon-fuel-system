# บันทึกงาน Re-layout — เฮงเจริญพืชผล

## สิ่งที่เปลี่ยนใหม่

- เปลี่ยนชื่อและภาพลักษณ์ทั้งหมดเป็น “เฮงเจริญพืชผล”
- สร้างโลโก้และ PWA icon ชุดใหม่ ไม่ใช้ไฟล์ SWT เดิม
- Desktop เปลี่ยนจาก Sidebar สีเข้มเป็น Top Navigation สองชั้น
- Mobile ใช้ Header แบบย่อ + Bottom Navigation เลื่อนได้
- ฟอร์มบันทึกงานมี Step Rail ทางซ้ายบนจอใหญ่ และเรียงเป็นขั้นตอนบนมือถือ
- ปรับสีเป็นเขียวพืชผล ทอง และครีม เพื่อลดความล้าในการมอง
- ปรับ Card, Input, Button, Date/Time picker, Upload และ Receipt ทั้งระบบ
- ใบสรุป PNG เปลี่ยนชื่อ โลโก้ สี และชื่อไฟล์เป็น HENG
- เปลี่ยนชื่อบริการ Render และฐานข้อมูลเริ่มต้นให้เป็นของเฮงเจริญพืชผล

## ระบบเดิมที่ยังอยู่ครบ

- Login และการแบ่งสิทธิ์ Owner / Employee
- Dashboard และตัวกรองวันที่
- บันทึก แก้ไข ลบ และดูรายการย้อนหลัง
- คำนวณจำนวนบาทและอัตราสิ้นเปลือง
- อัปโหลดรูปหลายไฟล์และบีบอัดรูปก่อนส่ง
- Draft อัตโนมัติแยกตามเครื่อง
- สต๊อกและประวัติการเติม
- รถ คนขับ พนักงาน และแจ้งเตือน
- Realtime ด้วย Socket.IO
- ดาวน์โหลดใบสรุปเป็น PNG
- Backend MongoDB และ API endpoint เดิม

## หมายเหตุการย้ายข้อมูล

ค่าเริ่มต้นของฐานข้อมูลใหม่คือ `heng_charoen_fuel_system` เพื่อไม่ให้ปะปนกับระบบเดิม หากต้องการใช้ข้อมูลเก่า ให้ตั้งตัวแปร `MONGODB_DB` เป็นชื่อฐานข้อมูลเดิมใน Render หรือไฟล์ `.env`
