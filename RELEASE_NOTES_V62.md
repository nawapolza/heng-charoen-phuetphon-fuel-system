# Release Notes V62 — Multi-Branch Professional Edition

เวอร์ชัน: **62.0.0**  
Build: `heng-charoen-v62-multi-branch`

## 1. ระบบหลายสาขา

- เพิ่ม Collection `branches` พร้อมรหัส ชื่อ ที่อยู่ เบอร์โทร หมายเหตุ สถานะ และสาขาหลัก
- เพิ่ม API เพิ่ม/ดู/แก้ไข/ปิดใช้งานสาขา
- สาขาใหม่จะได้รับสต๊อกเริ่มต้น 3 ประเภทโดยอัตโนมัติ: ดีเซล น้ำมันเครื่อง และแอดบลู
- สาขาที่ปิดใช้งานยังเก็บข้อมูลย้อนหลังครบถ้วน ไม่ลบประวัติทางบัญชี
- ระบบไม่อนุญาตให้ปิดสาขาสุดท้าย หรือสาขาที่ยังมีพนักงานใช้งานอยู่

## 2. การแยกข้อมูลแบบมืออาชีพ

ข้อมูลต่อไปนี้ผูกกับ `branch_id`, `branch_code` และ `branch_name`:

- สต๊อกและการตั้งค่าถัง
- การเคลื่อนไหวสต๊อกและรายการเติมคลัง
- การตรวจนับจริง
- รายการงานน้ำมัน
- Dashboard และรายงานประจำเดือน
- รถ/คนขับ
- บัญชีพนักงาน
- แจ้งเตือน

เจ้าของเลือกสาขาผ่าน `X-Branch-Id`; พนักงานถูกบังคับใช้สาขาจากบัญชีของตน ฝั่ง Backend ตรวจสิทธิ์ซ้ำทุก Route สำคัญ

## 3. การย้ายข้อมูล V61

เมื่อ Backend เชื่อม MongoDB ครั้งแรก ระบบจะ:

1. สร้างสาขา `สำนักงานใหญ่` รหัส `HQ` หากยังไม่มี
2. เติม `branch_id`, `branch_code`, `branch_name` ให้ข้อมูลเดิมที่ยังไม่มีสาขา
3. เปลี่ยน Unique Index ของสต๊อกจาก `item_type` เป็น `{ branch_id, item_type }`
4. สร้างสต๊อกที่ขาดให้ครบทุกสาขา โดยไม่ลบยอดเดิม

## 4. UX/UI ที่ปรับปรุง

- เพิ่มตัวเลือกสาขาที่ Sidebar, Topbar และเมนูมือถือ
- เพิ่มหน้า “จัดการสาขา” พร้อมการ์ดสาขาและฟอร์ม CRUD รวมการเปิดใช้งานสาขาที่เคยปิด
- เพิ่มแถบระบุสาขาในรายการย้อนหลัง เครื่องคำนวณ รถ พนักงาน แจ้งเตือน รายงาน และหน้าสต๊อก
- หน้าสต๊อกแบ่งทางลัดชัดเจน: ภาพรวมถัง, เติมเข้าคลัง, ตรวจนับจริง, ตั้งค่าถัง และประวัติ
- ฟอร์มบันทึกงานแสดงสาขาปลายทาง และเก็บ Draft แยกตามสาขา
- CSV/PDF รายงานระบุชื่อและรหัสสาขา พร้อมชื่อไฟล์แยกสาขา

## 5. การแก้ไขความเชื่อมโยง

- รถที่ไม่ผูกพนักงานถือเป็น “รถใช้ทั่วไป” ของสาขา พนักงานในสาขาเลือกใช้ได้ และเจ้าของสามารถล้างผู้รับผิดชอบรถได้
- ป้องกันทะเบียนรถซ้ำภายในสาขาเดียวกัน
- การแก้ไข/ลบบัญชี รถ รายการ และแจ้งเตือนตรวจ `branch_id` ป้องกันแก้ข้อมูลข้ามสาขา
- ย้ายพนักงานไปสาขาใหม่ได้โดยไม่ลบประวัติเดิม และปลดรถในสาขาเดิมเป็นรถใช้ทั่วไปอัตโนมัติ
- เปิดใช้งานบัญชีพนักงานที่เคยปิดได้จากหน้าเดียวกัน
- แก้การคืนสต๊อกตอนเปลี่ยนประเภทรายการ เพื่อไม่คืนยอดที่ไม่เคยถูกตัด
- เพิ่ม Migration ให้ `stock_transactions` เดิมด้วย

## 6. การตรวจสอบก่อนส่งมอบ

ใช้คำสั่ง:

```bash
npm run verify
npm run verify:v62
npm run build
```

ตรวจ Backend syntax, JavaScript/JSX, JSON, lockfile, Merge markers, Relative imports และโครงสร้าง Route หลายสาขา รายละเอียดผลและข้อจำกัด Production Build อยู่ใน `TEST_REPORT_V62.md`
