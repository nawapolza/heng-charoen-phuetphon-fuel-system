# Mobile UX v45

เวอร์ชันนี้ปรับประสบการณ์ใช้งานบนมือถือใหม่ทั้งระบบ โดยไม่เปลี่ยน API หรือโครงสร้างฐานข้อมูลเดิม

## จุดที่ปรับหลัก

- เพิ่มระยะปลอดภัยรอบหน้าจอและรองรับพื้นที่รอยบาก/แถบ Home Indicator
- เปลี่ยนเมนูมือถือเป็นแถบเมนูหลักแบบคงที่ ไม่ต้องเลื่อนแนวนอนและไม่หายระหว่างใช้งาน
- เพิ่มแผง “เมนูทั้งหมด” แบบ Bottom Sheet พร้อมปุ่มขนาดใหญ่และชื่อเมนูภาษาไทยชัดเจน
- ช่องกรอกบนมือถือเรียงหนึ่งคอลัมน์ อ่านง่าย กดง่าย และใช้ขนาดตัวอักษร 16px เพื่อป้องกัน Safari ซูมเอง
- ปุ่มหลักและพื้นที่สัมผัสมีความสูงเหมาะกับผู้สูงอายุ พร้อม Focus state สำหรับคีย์บอร์ดและอุปกรณ์ช่วยเหลือ
- ปรับฟอร์ม ตาราง การ์ด ตัวกรอง หน้าต่าง Modal ใบเสร็จ และหน้าเข้าสู่ระบบไม่ให้ล้นขอบจอ
- รองรับหน้าจอเล็กตั้งแต่ประมาณ 320px รวมถึงมือถือแนวนอนและแท็บเล็ตแบบสัมผัส
- เปิดให้ผู้ใช้ซูมหน้าเว็บได้ตามมาตรฐานการเข้าถึง
- เปลี่ยนข้อความทางเทคนิคบางส่วนเป็นภาษาไทยที่ผู้ใช้งานทั่วไปเข้าใจง่าย

## ไฟล์สำคัญที่แก้ไข

- `frontend/src/components/Layout.jsx`
- `frontend/src/index.css`
- `frontend/src/pages/StockPage.jsx`
- `frontend/src/pages/UsersPage.jsx`
- `frontend/index.html`

## การติดตั้งและรัน

```bash
npm run install:all
npm run dev:backend
npm run dev:frontend
```

สำหรับ Production:

```bash
npm run build
npm start
```

ใช้ Node.js 20 ตาม `.node-version` และ `package.json`
